import modules from './Settings.module.css'

const Settings = () => {
    return(
        <div className={modules.settings}>
            <span>Settings</span>
        </div>
    )
}

export default Settings